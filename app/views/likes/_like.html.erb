      <% if current_user.id != play.user_id %>
        <% if play.liked_by?(current_user) %>
          <p><%= link_to "お気に入り解除", play_likes_path(play), method: :delete, remote: true %></p>
        <% else %>
          <p><%= link_to "お気に入り登録", play_likes_path(play), method: :post, remote: true %></p>
        <% end %>
      <% end %>
      <%= play.likes.count %>